import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  reactStrictMode: true,
  experimental: {
    // Configurações experimentais se necessário
  },
  env: {
    // Variáveis de ambiente customizadas se necessário
  },
};

export default nextConfig;
